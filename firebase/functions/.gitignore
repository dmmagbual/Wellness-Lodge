node_modules/
lib/
*.log
# Generated by scripts/vendor-shared.cjs on every deploy -- the exact tarball
# it produces (and the dependency line it rewrites in package.json) get
# regenerated each time, so neither is meaningful to keep in git.
vendor/
